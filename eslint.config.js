import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import { defineConfig, globalIgnores } from 'eslint/config'

// Define and export the ESLint flat configuration array
export default defineConfig([
  // Globally ignore the 'dist' build directory from linting
  globalIgnores(['dist']),
  {
    // Target all JavaScript and JSX files across the project
    files: ['**/*.{js,jsx}'],
    // Extend recommended rulesets for JavaScript, React Hooks, and Vite Fast Refresh
    extends: [
      js.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
      parserOptions: {
        ecmaVersion: 'latest',
        ecmaFeatures: { jsx: true },
        sourceType: 'module',
      },
    },
    rules: {
      'no-unused-vars': ['error', { varsIgnorePattern: '^[A-Z_]' }],
    },
  },
])
